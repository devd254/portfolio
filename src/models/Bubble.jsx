import React from "react"

const Bubble = ({ position, size }) => {
    const {width, height} = size;
    const {x, y} = position;
    return(
        // bg-gradient-to-r from-white to-cyan-300 
        <section className="">
            <div className="w-full h-full rounded-full: border-radius: 100px">
                ...
            </div>
        </section>
            
    )
}

export default Bubble;